export type BookingId = {
    firstName: string;
    lastName: string;
    bookingId: number;
}

export type Amenities = {
    wifi?: boolean;
    tv?: boolean;
    radio?: boolean;
    refreshments?: boolean;
    safe?: boolean;
    views?: boolean;
}